---

- name: Verify
  hosts: localhost
  connection: local
  vars:
    ansible_python_interpreter: '{{ ansible_playbook_python }}'
  tasks:
  - name: Assert that the url is reachable
    uri:
      url: '{{ url }}'
      status_code:
      - 200
